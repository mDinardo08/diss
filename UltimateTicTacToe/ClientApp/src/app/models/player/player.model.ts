import { PlayerType } from "./player.type.enum";
import { PlayerColour } from "./player.colour.enum";

export class Player {

    public type: PlayerType;
    public name: string;
    public colour: PlayerColour;
}
